<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include("_head") %>
  </head>
  <body>
    <%- include("_header") %>

    <div class="container">
      <!-- FORMULÁRIO HTML -->
      <h2 class="nome">Editing Song</h2>
    <p>Que bom que você quer contribuir com nossa comunidade!!
      Edite uma música informando nome , ano de lançamento e banda.
      Obrigada pela contribuição!!
    </p>
      <form
        action="/update-song/<%= song.id %>"
        method="post"
      >
        <div class="mb-3">
          <label for="name" class="form-label">Name</label>
          <input
            type="text"
            class="form-control"
            name="name"
            aria-describedby="nome da música"
            value="<%= song.name %>"
            required
          />
        </div>
       
        <div class="mb-3">
          <label for="year" class="form-label">Year</label>
          <input
            type="text"
            class="form-control"
            name="year"
            aria-describedby="ano da música"
            value="<%= song.year %>"
            required
          />
        </div>
        <div class="mb-3">
          <label for="duration" class="form-label">duration</label>
          <input
            type="text"
            class="form-control"
            name="duration"
            aria-describedby="duração da música"
            value="<%= song.duration %>"
            required
          />
        </div>
        <div class="mb-3">
          <label for="bandId" class="form-label">Band</label>
          <select
            class="form-select"
            name="bandId"
            aria-label="Selecionar banda da música"
          >
            <option>- Select the band</option>
            <% bands.forEach(band => { %>
              <option
                value="<%= band.id %>"
                <%= band.id === song.bandId ? "selected" : "" %>
              >
              <%= band.name %>
            </option>
            <% }) %>
          </select>
        </div>
        <button type="submit" class="btn btn-primary">Save</button>
      </form>
    </div>]
    <footer class="footer">
      <p>
        <a href="https://www.instagram.com/tabernadometal/">@tabernadometal </a>
      </p>
      
    </footer>
  </body>
</html>